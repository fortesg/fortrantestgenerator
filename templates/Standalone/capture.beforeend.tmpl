$prologue $mergeBegin('ftg')
$mergeBegin('ftg')
IF (ftg_${subroutine.name}_capture_output_enabled .AND. ftg_${subroutine.name}_capture_active()) THEN
  CALL ftg_${subroutine.name}_capture_output($commaList($args.allOut, $result))
END IF

$epilogue $mergeEnd('ftg')